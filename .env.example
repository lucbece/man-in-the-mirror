# Optional: the control panel writes these into data/config.json instead.
# Values saved through the UI take precedence over anything set here.

# Bot token from https://discord.com/developers/applications → Bot → Reset Token
DISCORD_TOKEN=

# Optional: register slash commands instantly on one server instead of globally
DISCORD_GUILD_ID=

# Transcription. Needed for the OpenAI route; unused when running locally.
OPENAI_API_KEY=

# How much conversation to hold in memory, in seconds
BUFFER_SECONDS=180

# Speaking volume, 0.0 – 2.0
VOLUME=0.6

# Control panel
WEB_PORT=3000
WEB_HOST=127.0.0.1
